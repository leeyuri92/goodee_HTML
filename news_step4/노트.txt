학습목표 : 반복되는 코드를 제거하자.
개선코드 1단계 > 함수를 선언하자
해커뉴스 서버에 뉴스정보를 요청할 때  사용함 -> 응답(Array 30) 요청함
리턴값을 받을 수 있는 코드 추가됨
해커뉴스가 응답으로 제이슨 포맷으로 데이터셋을 준다.

ajax.open('GET', TITLE_URL, false);   
ajax.send();

ajax.open('GET', CONTENT_URL.replace('@id', id), false);
ajax.send();


변수선언 - 레스트풀
함수명 = (파라미터자리 or 빈함수) => {
  실행문(변수선언, 재정의, 제어문)
}
함수이름을 변수처럼 사용가능하다 (자바와의 차이점)
타입이 있지만 런타임시에 결정됨
또 컴파일이 없으니까(문법체크를 안하니까 독이 될 수도 있다.)
getData = (url) => {
  return JSON.parse(ajax.response);       JSON > Array
}

2단계 > 다음 위치를 결정하기
절차지향적이므로 순서도대로 최대한 처리해 볼 수 있도록 설계 (<->구조지향적이다. 자바는 메소드순서가 뒤죽박죽이여도 호출이 잘 된다.)
사용보다 선언이 먼저다.
파라미터를 결정한다. -> 사용자가 입력한 값을 담는다.
응답을 담는다 -> 리턴타입

3단계 >  실행되는 순서대로 코드를 나열하기



